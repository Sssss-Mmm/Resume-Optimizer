# Resume Optimizer Backend

FastAPI backend for the Resume Optimizer agentic service.

## Setup

```bash
uv sync
```

## Run

```bash
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
